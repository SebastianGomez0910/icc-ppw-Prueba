import { Routes } from '@angular/router';
import { HeroComponent } from './hero/heroComponent/heroComponent';

export const routes: Routes = [
    {
        path: 'inicio',
        component: HeroComponent
    }
];
